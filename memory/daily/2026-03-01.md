# 2026-03-01 工作日志

## 主题：Context Mode MCP 接入 + FTS5 搜索实验

### 背景
研究 claude-context-mode 项目，探索其对 OpenClaw 的价值。

### 完成事项

#### 1. Context Mode MCP 接入
- 通过 mcporter 成功接入 `context-mode` MCP 服务
- 通过 openclaw-mcp-adapter 配置到 openclaw.json
- 验证工具：`context-mode_execute` 可正常调用

#### 2. FTS5 知识库搭建
- 创建定时索引脚本：`~/workspace/scripts/index_skills_fts5.sh`
- 索引范围：skills/* + openclaw.json
- 定时任务：每天 23:00 执行（crontab）
- 测试搜索：✅ "mcporter add 命令" 精确命中

#### 3. 搜索策略分工
| 索引 | 用途 |
|------|------|
| Vector | 记忆检索（memory/*） |
| FTS5 | 精确搜索（skills/配置/代码） |

#### 4. 唤起精确搜索
- 指令方式："精确搜 xxx"、"FTS5 搜 xxx"、直接报关键词

### 工具对比
| 维度 | Vector 向量搜索 | FTS5 全文搜索 |
|------|----------------|---------------|
| 原理 | 语义理解 | 关键词匹配 |
| 精度 | 模糊 | 精确 |
| 成本 | API 调用 | 零成本 |
| 速度 | 慢 | 快 |

### 待优化
- [ ] FTS5 搜索 fallback 到 Vector（精确没结果→模糊兜底）
- [x] 定时任务日志目录创建

### 5. 省钱配置（2026-03-01 晚）
- 禁用心跳：`agents.defaults.heartbeat.every: "0m"`
- Gateway 已重启生效 ✅

### 6. Superpowers 集成
- 安装：✅ 已装（v4.3.1）
- 测试：⚠️ 能加载 skill，但遇到 API 400 错误
- 原因：Claude Code thinking 模式 + omnirouter/kimi 不兼容
- 状态：**遗留问题，待解决**

### 7. 定时任务（cron）
| 时间 | 任务 |
|------|------|
| 23:00 | FTS5 索引（index_skills_fts5.sh） |
| 23:50 | TaskFlow 逾期检查 |

### 关键文件
- `/home/nmaster/.openclaw/workspace/scripts/index_skills_fts5.sh` - 索引脚本
- cron: `0 23 * * * .../index_skills_fts5.sh`

---
**记录人**: 阿九

### 待解决：Superpowers + Claude Code + omnirouter/kimi thinking 兼容性问题

**问题**：Claude Code thinking 模式要求 reasoning_content 字段，但 kimi-k2.5 不返回该字段，导致 API 400 错误。

**已尝试方案**：
- alwaysThinkingEnabled: false ❌
- CLAUDE.md 注入 ❌
- 环境变量 ANTHROPIC_THINKING_ENABLED=false ❌

**根因**：thinking 在 Claude Code 内部硬编码，无法通过配置关闭

**解决方案**：
- 方案二：在 OmniRouter 代理层添加 middleware 伪造 reasoning_content
- 方案三：换用官方模型（Sonnet/Opus）

**状态**：待解决
